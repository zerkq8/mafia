-- =========================================================
-- MAFIA KUWAIT — أفاتار اللاعبين بالوضع المحلي (لم يكن موجودًا أصلًا)
-- نفّذ هذا في Supabase → SQL Editor
-- =========================================================

alter table players
  add column if not exists avatar_index int; -- 1..15، null للاعبين القدامى/الحكم
